/**
 * mul - function that create a multiplication between two integers
 * @a: first multiplier
 * @b: second multiplier
 *
 * Result: multiplication result
 */

int mul(int a, int b)
{
return (a*b);
}
