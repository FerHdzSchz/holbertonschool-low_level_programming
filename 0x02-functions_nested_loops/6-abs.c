#include "main.h"

/**
 * _abs - Function that calculate absolute value of n.
 * @n: Number to test
 *
 * Return: Absolute value of n.
 */

int _abs(int n)
{
if (n < 0)
{
return (-n);
}
return (n);
}
