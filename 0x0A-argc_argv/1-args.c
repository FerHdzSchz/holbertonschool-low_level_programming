#include <stdio.h>
/**
 * main - Print number of arguments in the function
 *
 * @argc: Number of arguments
 * @argv: Argument list
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
argv = argv;
printf("%d", argc - 1);
return (0);
}
